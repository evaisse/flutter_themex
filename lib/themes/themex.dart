import 'package:flutter/material.dart';

class Themex extends ThemeExtension<Themex> {
  @override
  ThemeExtension<Themex> copyWith() => Themex();

  @override
  ThemeExtension<Themex> lerp(covariant ThemeExtension<Themex>? other, double t) => Themex();
}
